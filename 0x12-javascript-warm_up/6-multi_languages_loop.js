#!/usr/bin/node

const languagesArray = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let index = 0;

while (index < languagesArray.length) {
  console.log(languagesArray[index]);
  index++;
}
